app = 'yhmemo-youtube-agent'
primary_region = 'nrt'

[build]

[processes]
  app = "python agent.py"

[mounts]
  source = "youtube_agent_data"
  destination = "/app/data"

[env]
  PYTHONUNBUFFERED = "1"

[vm]
  memory = '512mb'
  cpu_kind = 'shared'
  cpus = 1
