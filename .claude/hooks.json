{
  "hooks": {
    "SessionStart": [
      {
        "command": "set -a && [ -f .env ] && source .env; set +a && source ./scripts/fetch-secrets.sh 2>&1 || echo '[hook] 시크릿 로드 실패 - .env 또는 SUPABASE_SERVICE_ROLE_KEY 확인 필요'",
        "description": "Supabase에서 공용 시크릿(GH_TOKEN, NOTION_API_KEY 등)을 로드합니다"
      }
    ]
  }
}
