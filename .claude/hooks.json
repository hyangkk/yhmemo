{
  "hooks": {
    "SessionStart": [
      {
        "command": "source ./scripts/fetch-secrets.sh 2>&1 || echo '[hook] 시크릿 로드 실패'",
        "description": "Supabase에서 공용 시크릿(GH_TOKEN, NOTION_API_KEY 등)을 로드합니다"
      }
    ]
  }
}
