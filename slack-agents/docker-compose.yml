version: "3.8"

services:
  agents:
    build: .
    container_name: slack-ai-agents
    restart: unless-stopped
    env_file:
      - .env
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
